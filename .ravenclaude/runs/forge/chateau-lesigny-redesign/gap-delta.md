# G3 — Gap-Delta (A vs B)

## Strong agreements (→ feed the G4a critic for CORRELATED error)
- **Framework:** Astro 4.x + native folder i18n (`/[locale]/`, fr default). Both.
- **Inquiry funnel:** multi-step, branch by stream (wedding/film/photo), context pre-select, serverless fn → Turnstile → Resend (team + localized auto-reply), NO payment / soft date-check. Both.
- **Galleries:** PhotoSwipe v5, dynamic-import/lazy. Both.
- **Analytics:** cookieless, no consent banner (A: Cloudflare Web Analytics/Plausible; B: Plausible). Both.
- **Palette:** warm stone/limestone + oxblood/bronze accent, **explicitly NOT gold**; dark surface used sparingly. Both.
- **Type:** **anti-Playfair**, editorial serif display + grotesque UI + (A adds) mono slate. Both.
- **Signature = the décors** promoted to a first-class, distinctive surface. Both (different execution).
- **Motion:** Astro View Transitions + GSAP only on the signature moment + reduced-motion. Both.
- **Map:** NOT Google (GDPR) — A: MapLibre, B: Mapbox GL, both custom monochrome style. Both.
- **360:** lazy, conditional per-décor (A: Kuula/Matterport, B: Pannellum). Both.
- **Phased plan** with a **Phase-0 asset gate** and the **photography/illustration capture as the hidden long-pole**. Both.

## Real conflicts (→ G4b tiebreak, ranked by impact)
| # | Conflict | Panel A | Panel B | Impact |
|---|---|---|---|---|
| C1 | **CMS** | **Sveltia** (git-based, free, i18n-first, no DB) | **Storyblok** (visual editor, hosted, free-1-user, €99/mo for >1) | **HIGH** — decisive fork |
| C2 | **Signature execution** | **Décor Index** — filterable editorial catalogue (buildable NOW from photos) | **Cartography Map** — commissioned hand-survey SVG floor plan (illustration on critical path) | **HIGH** — distinctiveness vs dependency |
| C3 | **Hosting/serverless** | **Cloudflare** Pages + Functions | **Netlify** + Edge Functions | MED — partly follows C1 |
| C4 | **Typography** | **GT Sectra + Söhne + GT Pressura Mono** (paid; free fallbacks Fraunces/Newsreader/Instrument/Geist) | **Cormorant SC + Source Serif 4 + DM Sans** (all free) | MED — budget vs distinctiveness |
| C5 | **Map lib** | MapLibre GL (open, no token) | Mapbox GL (token, free tier) | LOW — default MapLibre |
| C6 | **360 lib** | Kuula/Matterport embed (turnkey, paid/branded) | Pannellum (free, self-host, needs equirect images) | LOW — conditional either way |

## Where A is silent / B over/under-specifies
- B specifies a **50-day human-team timeline** + commissioned illustrator + pro video shoot + client training; A is lighter on calendar but same long-pole.
- B assumed A would pick Tailwind; **A actually also used custom-property tokens** → no real CSS conflict (both lean vanilla/tokens).
- Neither panel reckons with the actual execution context: **Claude builds this NOW in an empty codespace** — no budget line, no illustrator, no photographer, no SaaS accounts pre-provisioned. (→ premise for G4a.)
