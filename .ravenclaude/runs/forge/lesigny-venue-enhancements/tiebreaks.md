# G4b — Tiebreaks (synthesizer rulings; low-blast, defaulted per critic+B)
D1 style quiz → DROP (duplicate taxonomy vs existing décor filter; maintenance trap). 
D2 season-by-month guide → DROP (evergreen copy ages; no maintenance owner).
D3 capacity/layout visualizer → DEFER (medium build; keep core tight per "leaner" directive).
D4 FAQ placement → dedicated /faq page (clean FAQPage schema) linked from footer + contact + mariages;
   NOT added to the already-8-item top nav.
D5 real-weddings → SECTION on mariages (not a new page), existing imagery, honest captions.
D6 empty-frame cap → moot: re-scope ships ZERO empty frames (only real-content features this session).
Backend fork → fully STATIC this session (keep mailto; fix optional-endpoint logic). Web3Forms/serverless
   deferred to go-live WITH a required privacy-policy rewrite. (User corrected to "option 1" = static + subset;
   matches critic CE-1/CE-3 + premise.)
