# Panel B (sonnet) — condensed
Leaner cut of the same architecture. Verified: contact.astro already reads PUBLIC_INQUIRY_ENDPOINT and
POSTs before mailto; mariages.astro:405-424 is an existing honest empty-state precedent; DomaineSchematic
has named <g id="zone-*"> groups and DomaineIndex has decors[] with space/uses — map is a wiring exercise;
pdfkit installed but unused, no scripts/ dir; only TouristAttraction JSON-LD; site.weddings has one price
(3350); no consent component; GH Pages hosting (migration = net-new decision).
Same Phase 0/1 as A. DROPS from "full set": style quiz (duplicate taxonomy), season guide (evergreen copy
ages), capacity/layout visualizer (seating-planner-adjacent), heavy IG embeds; merges 360+video into one
hero-media slot. Prefers FAQ/vouchers EMBEDDED in existing pages over new pages (nav already long).
GAP-DELTA: don't over-serialize on "pick a backend" (mailto already degrades); don't over-build pricing
engine / pan-zoom map / bespoke newsletter CRM. RISKIEST ASSUMPTION: many simultaneous null-gated "coming
soon" frames read as *unfinished*, not restraint, on a luxury venue → cap <=2 visible at once; and owner
asset-supply cadence is asserted, not demonstrated. Full verbatim in transcript agent af42b35c19ea94edf.
