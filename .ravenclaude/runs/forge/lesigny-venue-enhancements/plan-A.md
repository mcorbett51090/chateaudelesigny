# Panel A (opus) — condensed
Maximalist "full set" as config-flag frames. Forks: forms=Web3Forms-on-GH-Pages (defer Cloudflare);
gated=features.ts flags. Phase 0 prereqs: schema.ts + BaseLayout jsonLd prop; ConsentEmbed.astro (CNIL);
features.ts; i18n keys. Phase 1 ship-now: Web3Forms wiring + auto-reply; "book a visit" funnel; WhatsApp
wa.me; faq.astro (new page) + FAQPage schema; per-page schema (EventVenue/LodgingBusiness/Product);
cookieless analytics; interactive estate map (DomaineSchematic→DomaineIndex filter); build-time brochure
PDF (scripts/generate-brochure.mjs + prebuild) + BrochureGate soft-gate. Phase 2: featured-weddings page;
reviews frame+schema(off till real); season-by-month page; décor quiz; gift vouchers (Stripe links);
newsletter (Brevo); capacity visualizer. Phase 3 asset-gated frames: before/after slider; 360 (ConsentEmbed);
video hero; floor-plan+menu welcome pack. DAG: schema+jsonLd prop and ConsentEmbed are the shared roots;
⚠ verify withastro/action runs npm prebuild (else brochure PDF missing in CI). Full verbatim in run transcript
agent a57d8040a205a4c11.
